apiVersion: v1
kind: Secret
metadata:
  name: sta-spring-boot-trading-agent-config
type: Opaque
stringData:
  provider: sandbox
  type: config
  app.marketplace.url: https://sta.az.run.withtanzu.com
  app.agent.strategy: random
  app.agent.user: mandela-users
